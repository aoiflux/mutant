# Automation Examples

Run from repository root (compile, then run the bytecode it writes
beside the source):

```bash
mutant gen --src examples/automation/pipeline_end_to_end.mut --password <password>
mutant examples/automation/pipeline_end_to_end.mu --dev --password <password>
```

Scripts:
- pipeline_end_to_end.mut
