# Cache Examples

Run from repository root (compile, then run the bytecode it writes
beside the source):

```bash
mutant gen --src examples/cache/cache_example.mut --password <password>
mutant examples/cache/cache_example.mu --dev --password <password>
```

Scripts:
- cache_example.mut
- cache_ttl_workflow.mut
