# Macro Examples

These examples demonstrate Mutant macro syntax and macro-expansion patterns.

Run from repository root, for example:

```powershell
mutant examples/macros/macro_quote_unquote_basics.mut --password test123
```

These examples compile and run through the normal source-file execution path.
