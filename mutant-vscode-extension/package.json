{
    "name": "mutant-language-tools",
    "displayName": "Mutant Language Tools",
    "description": "Language tools for Mutant, powered by a dedicated language server: strict canonical formatting, rich diagnostics (OS-aware platform warnings, unreachable code, unused/undefined/duplicate checks, semicolons), hover with capability categories, completion, semantic highlighting, go-to-definition, references, and rename.",
    "version": "0.2.0",
    "publisher": "aoiflux",
    "icon": "images/icon-128.png",
    "license": "AGPL-3.0-only",
    "repository": {
        "type": "git",
        "url": "https://github.com/aoiflux/mutant.git"
    },
    "engines": {
        "vscode": "^1.90.0"
    },
    "categories": [
        "Programming Languages",
        "Linters",
        "Formatters",
        "Snippets"
    ],
    "keywords": [
        "mutant",
        "forensics",
        "dfir",
        "security",
        "language server",
        "lsp",
        "formatter",
        "linter"
    ],
    "main": "./out/extension.js",
    "contributes": {
        "languages": [
            {
                "id": "mutant",
                "aliases": [
                    "Mutant",
                    "mutant"
                ],
                "extensions": [
                    ".mut"
                ],
                "configuration": "./language-configuration.json"
            }
        ],
        "grammars": [
            {
                "language": "mutant",
                "scopeName": "source.mutant",
                "path": "./syntaxes/mutant.tmLanguage.json"
            }
        ],
        "snippets": [
            {
                "language": "mutant",
                "path": "./snippets/mutant.json"
            }
        ],
        "breakpoints": [
            {
                "language": "mutant"
            }
        ],
        "debuggers": [
            {
                "type": "mutant",
                "label": "Mutant",
                "languages": [
                    "mutant"
                ],
                "configurationAttributes": {
                    "launch": {
                        "required": [
                            "program"
                        ],
                        "properties": {
                            "program": {
                                "type": "string",
                                "description": "The .mut source file to debug. Debugging runs from source: a .mu built for release carries no source positions, so there is nothing to step through.",
                                "default": "${file}"
                            },
                            "stopOnEntry": {
                                "type": "boolean",
                                "description": "Stop before the first instruction, so the program can be stepped from the beginning with no breakpoint set.",
                                "default": false
                            },
                            "modulePaths": {
                                "type": "array",
                                "items": {
                                    "type": "string"
                                },
                                "description": "Directories to search for imported modules, in order. The launch configuration's spelling of --module-path.",
                                "default": []
                            },
                            "noDebug": {
                                "type": "boolean",
                                "description": "Run the program without stopping for anything. Output and the exit code are still reported.",
                                "default": false
                            }
                        }
                    }
                },
                "initialConfigurations": [
                    {
                        "type": "mutant",
                        "request": "launch",
                        "name": "Debug the current Mutant file",
                        "program": "${file}",
                        "stopOnEntry": false
                    }
                ],
                "configurationSnippets": [
                    {
                        "label": "Mutant: debug the current file",
                        "description": "Compile the open .mut file with positions and debug it.",
                        "body": {
                            "type": "mutant",
                            "request": "launch",
                            "name": "Debug the current Mutant file",
                            "program": "^\"\\${file}\"",
                            "stopOnEntry": false
                        }
                    }
                ]
            }
        ],
        "taskDefinitions": [
            {
                "type": "mutant",
                "required": [
                    "command"
                ],
                "properties": {
                    "command": {
                        "type": "string",
                        "enum": [
                            "gen",
                            "run",
                            "release"
                        ],
                        "description": "gen = compile a .mut to encrypted .mu bytecode; run = execute a compiled .mu; release = build a standalone executable."
                    },
                    "src": {
                        "type": "string",
                        "description": "Source (.mut) or bytecode (.mu) file. Defaults to the active editor file."
                    },
                    "os": {
                        "type": "string",
                        "enum": [
                            "windows",
                            "linux",
                            "darwin"
                        ],
                        "description": "release only: target operating system."
                    },
                    "arch": {
                        "type": "string",
                        "enum": [
                            "amd64",
                            "arm64"
                        ],
                        "description": "release only: target architecture."
                    },
                    "mutation": {
                        "type": "number",
                        "description": "Polymorphic mutation level (0-10)."
                    },
                    "password": {
                        "type": "string",
                        "description": "Optional encryption/decryption password."
                    },
                    "args": {
                        "type": "array",
                        "items": {
                            "type": "string"
                        },
                        "description": "Additional raw CLI arguments appended to the command."
                    }
                }
            }
        ],
        "commands": [
            {
                "command": "mutant.openSmokeFile",
                "title": "Mutant: Open Smoke File",
                "category": "Mutant"
            },
            {
                "command": "mutant.runSmokeChecks",
                "title": "Mutant: Run LSP Smoke Checks",
                "category": "Mutant"
            },
            {
                "command": "mutant.showLspStatus",
                "title": "Mutant: Show LSP Status",
                "category": "Mutant"
            },
            {
                "command": "mutant.showLspLogs",
                "title": "Mutant: Show LSP Logs",
                "category": "Mutant"
            },
            {
                "command": "mutant.restartLsp",
                "title": "Mutant: Restart LSP",
                "category": "Mutant"
            },
            {
                "command": "mutant.copyLspLogs",
                "title": "Mutant: Copy LSP Logs",
                "category": "Mutant"
            },
            {
                "command": "mutant.formatDocument",
                "title": "Mutant: Format Document",
                "category": "Mutant"
            }
        ],
        "configuration": {
            "title": "Mutant",
            "properties": {
                "mutant.languageServer.path": {
                    "type": "string",
                    "default": "",
                    "description": "Path or command name for the Mutant language server executable. Leave empty to auto-detect packaged platform binary first, then workspace/PATH."
                },
                "mutant.languageServer.args": {
                    "type": "array",
                    "default": [],
                    "description": "Additional command line arguments passed to the Mutant language server.",
                    "items": {
                        "type": "string"
                    }
                },
                "mutant.cli.path": {
                    "type": "string",
                    "default": "mutant",
                    "description": "Path or command name for the Mutant CLI, used by the Mutant build/run/release tasks. The CLI is separate from the bundled language server and is expected on PATH by default."
                },
                "mutant.strictFormatting": {
                    "type": "boolean",
                    "default": true,
                    "description": "Format Mutant files using the canonical style: four-space indentation, spaces only, and a semicolon after every statement that requires one. This is a master on/off switch, not a style option — Mutant formatting is deliberately not configurable, so that the same source always produces the same bytes. Set to false to disable Mutant formatting entirely."
                },
                "mutant.format.onType.enabled": {
                    "type": "boolean",
                    "default": false,
                    "description": "Enable on-type formatting for Mutant files. Off by default: on-type formatting reformats the whole document on every ';', '}' and newline, which is disruptive while typing. Format-on-save is enabled by default instead."
                },
                "mutant.lint.rules.duplicateTopLevelDeclaration.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for duplicate top-level declaration lint diagnostics. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.unusedDeclaration.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for unused top-level declaration lint diagnostics. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.unusedImport.severity": {
                    "type": "string",
                    "default": "information",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for an import whose namespace is never read. Information by default: the imported module's top-level statements run whether or not its namespace is used. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.undefinedDeclaration.severity": {
                    "type": "string",
                    "default": "error",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for undefined identifier lint diagnostics. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.semicolon.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for missing or redundant semicolon diagnostics. Both come with a quick fix. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.nestingComplexity.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for deep nesting diagnostics in function bodies. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.unreachableCode.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for unreachable-code diagnostics: statements after an unconditional return/break/continue, and a match arm written after `_`, which matches anything. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.platformSupport.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for platform-support diagnostics: warns when a program calls a builtin that is not supported on the operating system the language server is running on (for example a Windows/Linux-only builtin used on macOS). Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.builtinArity.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for builtin arity diagnostics: warns when a builtin with a known fixed argument count is called with the wrong number of arguments (for example abs(1, 2) or clamp(x)). Only a curated, verified subset of builtins is checked. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.builtinArgType.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for builtin argument-type diagnostics: warns when a builtin is passed a value of a type its parameter cannot accept (for example str_upper(42) or len(true)). Only builtins whose parameter types have been verified against their implementation are checked, and only arguments whose type is certain — a literal, or a name that is never reassigned. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.builtinSingleReturn.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for single-return binding diagnostics: warns when several names are bound from a builtin that returns one value rather than a (value, err) pair (for example let updated, err = push(items, x)). Nothing fails at runtime — if the value is an array the binding takes it apart, and otherwise the extra names are always null, so an if (err) check never fires. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.builtinPairReturn.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for pair-return binding diagnostics: warns when one name is bound from a builtin that returns a (value, err) pair (for example let data = fs_read(path)). The name holds the pair rather than the value, and nothing fails at the binding — a pair prints as its value, so the mistake surfaces somewhere else or not at all. Silent when the program does something with the name that only makes sense for a pair, such as indexing it or returning it. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.builtinDeprecated.severity": {
                    "type": "string",
                    "default": "hint",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for deprecated-builtin diagnostics: reports a call to a builtin that is kept only so existing programs keep working, and names its replacement (for example net_syn_scan, replaced by net_connect_scan). The call is correct and still runs, which is why this defaults to a hint rather than a warning. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.spawnGlobalWrite.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for spawned-callback global-write diagnostics: warns when a spawn, pmap or peach callback assigns to a top-level name. Those callbacks run on their own VM with a snapshot of the globals, so the write lands in a copy that is discarded when the callback finishes — the program runs, reports nothing, and the value never changes. Only a function literal written at the call site is checked, and only a name the callback does not rebind for itself. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.assignmentTarget.severity": {
                    "type": "string",
                    "default": "error",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for assignment-target diagnostics: reports the two assignment targets the compiler refuses. A target with no variable under it ([1, 2][0] = 9, f()[0] = 1) has nowhere to store its result, and an index before the last one is evaluated more than once on the way back out, so it has to be a name or a literal. Both are compile errors, so this reports them where they are written rather than at the next build. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.unclosedResource.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for unclosed-resource diagnostics: warns when a builtin that opens a handle (ntfs_open, zip_open, chan_new, net_connect and their families) binds it to a name nothing in the same scope closes. Handles live in a package-level store with no eviction, so an unclosed one is held for the life of the process — invisible in a script that exits, descriptor exhaustion in a loop or a long-running server. The rule stays quiet whenever the handle is used somewhere it cannot follow (returned, passed to a helper, stored) or the closer is named anywhere in the scope, including inside with_resource. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.uncheckedError.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for unchecked-error diagnostics: warns when `let value, err = f(...)` binds the error half of a (value, err) builtin and nothing reads that binding before the name is rebound or the scope ends. A failed call leaves null in the value beside the error, so a program that ignores the error carries on with nothing and reports success. The rule stays quiet when the error is read in any way at all \u2014 tested, printed, returned, passed on \u2014 when the failure is caught through the value instead (an `if` or `for` condition on it, or a BOOLEAN success flag used anywhere), when a nested function mentions the name, and when `_` is bound to say the failure is deliberately ignored. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.matchExhaustiveness.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for match-exhaustiveness diagnostics: warns when every arm of a match is a variant of one enum declared in the same file, no arm is `_`, and a variant of that enum has no arm. A match must produce a value, so a subject no arm matches raises at run time rather than yielding null — which means adding a variant to an enum silently leaves every existing match over it one arm short. The rule stays quiet unless it is certain: a literal pattern, two different enums, an imported enum (`mod.Status.Ok`, whose variants live in another file), a `_` arm, or a name that is both an enum and a `let` binding all suppress it. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.tlsVerificationDisabled.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for tls-verification-disabled diagnostics: warns when a TLS call is handed literal options that weaken it. `insecure: true` on `net_tls_connect` or `net_tls_upgrade_client` turns off certificate verification, leaving the connection encrypted but unauthenticated \u2014 anything on the path can present its own certificate, and the call still succeeds. A `min_version` of \"1.0\" or \"1.1\" (client or server) accepts versions RFC 8996 deprecated in 2021. The rule reads only literal options, inline or bound once to a name in the same scope, so an option whose value is computed is one it says nothing about; it never reports a missing `min_version`, because a default is the runtime's business. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.unboundedResource.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for unbounded-resource diagnostics: warns when `cidr_hosts` or `range` is given literal arguments larger than the builtin will return \u2014 more than 20 host bits of CIDR, or more than 10,000,000 elements of range. Neither is an unbounded allocation: both raise at run time, so `cidr_hosts(\"10.0.0.0/8\")` is a call that reads as a reasonable network sweep and simply fails. The rule evaluates exactly the predicate the builtin evaluates, and only on arguments written out as literals, so it is quiet on anything computed. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.weakCrypto.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for weak-crypto diagnostics: warns when MD5, SHA-1 or CRC-32 decides whether something is authentic. Matching an artifact against a known-file set is what these algorithms are for in a forensic tool, so the rule does not look at the algorithm \u2014 it looks at the decision: comparing a digest against one written into the program is verifying authenticity, and all three have long been forgeable by anyone who controls the input. Comparing two computed digests, storing one, printing one, or looking one up in a hash set stays quiet. An hmac() over \"md5\" or \"sha1\" is also reported, because keying a hash has only one purpose. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.hardcodedSecret.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for hardcoded-secret diagnostics: warns when a credential is written into the program. A literal is reported when its name says credential (password, token, api_key, client_secret \u2026) or its shape belongs to an issuer (AWS, GitHub, Slack, a PEM private key, a signed JWT), when the value reads as issued rather than as a word or a placeholder, and when the program does not hand it to a decoder. That last question is what separates a credential from a sample: a value whose fate is jwt_decode, base64_decode, pem_decode or x509_parse is being examined, not presented. A literal in a bare argument position carries no name signal. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.commandInjection.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for command-injection diagnostics: warns when a value is spliced into a string an interpreter will parse \u2014 interpolated or concatenated into the command exec_string hands to a shell, the line cmd_add adds to a builder, or the script lua_run_string runs. The interpreter parses the finished string, so the spliced value is syntax rather than an argument: a space adds a word, and ';', '|' or '$(...)' start a command of their own. A command built only from literals is quiet, and so is a value that first goes through url_encode, base64_encode, hex_encode, to_int, parse_int, text_replace or regex_replace. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.evidenceMutation.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for evidence-mutation diagnostics: warns when fs_write, fs_append, fs_delete, fs_move, or the destination of fs_copy, is aimed at a path the same program opened as evidence \u2014 raw_open, ewf_open, vhdi_open, a filesystem or hive opener, an archive. Evidence is written once and read forever: a hash taken afterwards no longer matches the one in the notes. The rule compares what was written rather than what it will evaluate to \u2014 the same identifier in the same scope, or the same string literal anywhere in the file \u2014 so a derived path or a containing directory is never reported, and fs_copy FROM the evidence, which is the correct procedure, stays quiet. Set to 'off' to suppress the rule."
                },
                "mutant.lint.rules.pathTraversal.severity": {
                    "type": "string",
                    "default": "warning",
                    "enum": [
                        "error",
                        "warning",
                        "information",
                        "hint",
                        "off"
                    ],
                    "description": "Severity for path-traversal diagnostics: warns when a filesystem path is built from a value the program did not write \u2014 gets(), serve_arg(), net_conn_read(), or an HTTP request or response \u2014 and nothing in the file looks at that value before it reaches fs_read, fs_write, a *_read_file or an archive entry. A '..' segment walks out of whatever directory the rest of the path names, so the file is chosen by whoever supplied the value. The taint follows one hop of 'let' through concatenation and interpolation, and stops at a function boundary. Any mention of the value as an argument to a text-inspecting builtin (text_contains, regex_match, str_starts_with, text_replace, hashset_contains \u2026) counts as the check and silences the report; the rule cannot read which characters were looked for and takes the author's word. Set to 'off' to suppress the rule."
                }
            }
        },
        "configurationDefaults": {
            "[mutant]": {
                "editor.formatOnSave": true,
                "editor.formatOnType": false
            }
        },
        "menus": {
            "commandPalette": [
                {
                    "command": "mutant.formatDocument",
                    "when": "editorLangId == mutant"
                }
            ],
            "editor/context": [
                {
                    "command": "mutant.formatDocument",
                    "when": "editorLangId == mutant",
                    "group": "1_modification"
                }
            ]
        },
        "keybindings": [
            {
                "command": "mutant.restartLsp",
                "key": "ctrl+alt+r",
                "mac": "cmd+alt+r",
                "when": "editorLangId == mutant"
            }
        ]
    },
    "scripts": {
        "prepare:lsp-bins": "node ./scripts/stage-lsp-binaries.mjs",
        "check:lsp-bins": "node ./scripts/stage-lsp-binaries.mjs --check",
        "esbuild": "node ./esbuild.mjs",
        "compile": "node ./esbuild.mjs --production",
        "watch": "node ./esbuild.mjs --watch",
        "compile-tests": "tsc -p ./",
        "vscode:prepublish": "npm run compile",
        "lint": "eslint src --ext ts",
        "pretest": "npm run compile-tests && npm run lint",
        "test": "node ./out/test/runTest.js",
        "package": "node ./scripts/package-targets.mjs"
    },
    "devDependencies": {
        "@types/mocha": "^10.0.10",
        "@types/node": "^20.14.2",
        "@types/vscode": "^1.90.0",
        "@typescript-eslint/eslint-plugin": "^8.2.0",
        "@typescript-eslint/parser": "^8.2.0",
        "@vscode/test-electron": "^2.5.2",
        "@vscode/vsce": "^3.2.0",
        "esbuild": "^0.28.1",
        "eslint": "^9.8.0",
        "glob": "^11.0.3",
        "mocha": "^12.0.0",
        "typescript": "^5.5.4"
    },
    "dependencies": {
        "vscode-languageclient": "^9.0.1"
    }
}
